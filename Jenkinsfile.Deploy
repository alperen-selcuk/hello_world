pipeline {
  agent any
  stages {
    stage ("get number") {
      steps {
        def buildNumber = Jenkins.instance.getItem('Build').lastSuccessfulBuild.number
        echo $buildNumber
      }
    }
  }
}
