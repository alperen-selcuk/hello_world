pipeline {
  environment {
    
  }
  agent any
  stages {
    stage ("get number") {
      steps {
        sh "build_id = $(wget -q0- http://jenkins-34-142-28-211.nip.io:8080/job/Build/lastSuccessfulBuild/buildNumber)"
        sh "echo ${build_id}"
      }
    }
  }
}
