pipeline {
  environment {
    build_id = "wget -q0- http://jenkins-34-142-28-211.nip.io:8080/job/Build/lastSuccessfulBuild/buildNumber"
  }
  agent any
  stages {
    stage ("get number") {
      steps {
        sh "echo ${build_id}"
      }
    }
  }
}
