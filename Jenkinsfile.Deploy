pipeline {
  stages {
    stage ("get number") {
      steps {
        script {
          def lastbuild = Jenkins.instance.getItem(env.1.2 Build).lastSuccessfulBuild.displayName
          echo "Last Success Build Name: ${lastSuccessBuildName}"
        }
      }
    }
  }
}
