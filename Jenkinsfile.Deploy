pipeline {
  agent any
  stages {
    stage ("get number") {
      steps {
        sh "wget -q0- http://jenkins-34-142-28-211.nip.io:8080/job/Build/lastSuccessfulBuild/buildNumber"
      }
    }
  }
}
